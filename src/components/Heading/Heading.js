import styled from "styled-components";
import { theme } from "../../Theme/MainStyle";

const Heading = styled.h1`
  font-size: ${theme.bigFont};
  text-transform: uppercase;
  color: #fff;
  letter-spacing: 7px;
  font-family: "Permanent Marker", cursive;
  margin: 0 0 1em 0;
`;
export default Heading;
